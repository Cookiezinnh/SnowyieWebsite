<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('includes/head') %>
    <link rel="stylesheet" href="/styles/page_css/login.css">
</head>
<body>
    <%- include('includes/main-hero-displayonly') %>

    <section class="hero section-1">
        <div class="row container justify-items-c">
            <div class="text-align-c text-group">
                <h1 class="m-title t-bold">Log-in</h1>
                <p class="s-text">Utilize o seu nome de usuário e senha, para realizar o seu log-in em nossos serviços:</p>
                <form action="/login" method="POST">
                    <br>
                    <div class="label-div">
                        <label for="username" class="s-text">Nome de Usuário ou Email:</label>
                    </div>
                    <input type="text" id="username" name="username" class="txt-cmp s-text btn-large-auto txt-cmp-dark" placeholder="ex: PeixeAquatico ou email@example.com" autocomplete="off">
                    <br><br>
                    <div class="label-div">
                        <label for="password" class="s-text">Senha:</label>
                    </div>
                    <input type="password" id="password" name="password" class="txt-cmp s-text btn-large-auto txt-cmp-dark" placeholder="ex: ************" autocomplete="off">
                    <br><br>

                    <% if (errors && errors.length > 0) { %>
                        <% errors.forEach(error => { %>
                            <p class="s-text error"><%= error %></p>
                        <% }); %>
                    <% } %>

                    <br>
                    <a href="#">
                        <button class="btn s-text btn-large-auto btn-green">
                            Logar
                        </button>
                    </a>
                </form>
                <br>
                <a href="/register">
                    <p class="s-text cursor-pointer">Não possui uma conta?</p>
                </a>
            </div>
        </div>
    </section>    
</body>
</html>